spring:
  application:
    name: time-sheet-mngmnt-sys
  datasource:
    url: jdbc:mysql://localhost:3306/timesheet_db
    username: root
    password: hasan

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
